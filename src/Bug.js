/**
 * @class FW.kb.Bug
 * @uses FW.kd.Some
 */
export class {
	/**
	 * @param {FW.kb.Some|FW.kb.Some[]} d  // <<-- focus........
	 */
	dede(d) {
		if ( !d.length ) d.getIDProperty();
	};
}
